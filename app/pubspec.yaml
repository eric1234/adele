name: adele_desktop
description: The ADELE Flutter desktop application.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.10.9 <4.0.0"
  flutter: ">=3.38.10"

resolution: workspace

dependencies:
  agent_kernel: ^0.1.0
  adele_capabilities: ^0.1.0
  adele_contract: ^0.1.0
  adele_core_extensions: ^0.1.0
  adele_environment: ^0.1.0
  adele_model_tool: ^0.1.0
  adele_model_provider: ^0.1.0
  adele_orchestration: ^0.1.0
  adele_plugin_api: ^0.1.0
  adele_product: ^0.1.0
  adele_project_storage: ^0.1.0
  adele_ui: ^0.1.0
  crypto: ^3.0.6
  dart_eval: 0.8.5
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_eval: 0.8.2
  plugin_runtime: ^0.1.0
  sqlite3: ^3.5.2

dev_dependencies:
  adele_plugin_backend_support: ^0.1.0
  chat_strategy_backend: ^0.1.0
  chat_strategy_contract: ^0.1.0
  command_tools_plugin: ^0.1.0
  contract_codegen: ^0.1.0
  file_selector_platform_interface: ^2.7.0
  filesystem_tools_plugin: ^0.1.0
  openai_contract: ^0.1.0
  plugin_builder: ^0.1.0
  resource_inspector_contract: ^0.1.0
  scripted_model_contract: ^0.1.0
  search_tools_plugin: ^0.1.0
  workspace_demo_contract: ^0.1.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
