name: adele_model_tool
description: Public ADELE model-tool contribution and execution API.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_plugin_api: ^0.1.0
  adele_product: ^0.1.0

dev_dependencies:
  test: ^1.26.3
