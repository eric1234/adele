name: filesystem_tools_plugin
description: Stock ADELE model tools for Session-authorized Environment filesystems.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_environment: ^0.1.0
  adele_model_tool: ^0.1.0
  adele_plugin_api: ^0.1.0

dev_dependencies:
  agent_kernel: ^0.1.0
  adele_product: ^0.1.0
  test: ^1.26.3
