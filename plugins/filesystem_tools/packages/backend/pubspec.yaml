name: filesystem_tools_backend
description: ADELE Session-authorized Filesystem Tools backend.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_contract: ^0.1.0
  adele_environment: ^0.1.0
  adele_model_tool: ^0.1.0
  adele_plugin_backend_support: ^0.1.0
  adele_product: ^0.1.0
  filesystem_tools_plugin: ^0.1.0

dev_dependencies:
  test: ^1.26.3
