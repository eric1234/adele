name: chat_strategy_plugin
description: Stock ADELE Chat orchestration strategy and in-memory conversation state.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_orchestration: ^0.1.0
  adele_plugin_api: ^0.1.0

dev_dependencies:
  adele_product: ^0.1.0
  test: ^1.26.3
