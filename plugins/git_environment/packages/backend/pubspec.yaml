name: git_environment_backend
description: Git worktree-backed ADELE Environment provider.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_capabilities: ^0.1.0
  adele_contract: ^0.1.0
  adele_environment: ^0.1.0
  adele_plugin_api: ^0.1.0
  adele_product: ^0.1.0
  crypto: ^3.0.6

dev_dependencies:
  plugin_runtime: ^0.1.0
  test: ^1.26.3
