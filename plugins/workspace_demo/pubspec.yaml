name: workspace_demo
description: Workspace root for the ADELE workspace demo source plugin.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.12.2 <4.0.0"

resolution: workspace

workspace:
  - packages/*
