name: workspace_demo_frontend
description: Future interpreted Flutter frontend for the workspace demo plugin.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"
  flutter: ">=3.38.10"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  workspace_demo_contract: ^0.1.0
