name: scripted_model_backend
description: Deterministic scripted model fixture backend.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  scripted_model_contract: ^0.1.0

dev_dependencies:
  test: ^1.26.3
