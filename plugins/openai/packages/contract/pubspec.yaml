name: openai_contract
description: Shared OpenAI native replay and safe presentation identities.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dev_dependencies:
  test: ^1.26.3
