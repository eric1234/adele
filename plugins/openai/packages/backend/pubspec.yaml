name: openai_model_provider_backend
description: OpenAI Responses model-provider backend for ADELE.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_model_provider: ^0.1.0

dev_dependencies:
  test: ^1.26.3
