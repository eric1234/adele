name: openai_native_activity
description: Public OpenAI native activity identity and safe summary projection.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_orchestration: ^0.1.0

dev_dependencies:
  test: ^1.26.3
