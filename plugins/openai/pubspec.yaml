name: openai_plugin
description: ADELE OpenAI model-provider plugin workspace.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

workspace:
  - packages/backend
