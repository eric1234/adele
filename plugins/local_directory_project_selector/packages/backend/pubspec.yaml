name: local_directory_project_selector_backend
description: Local directory backing placement for ADELE Projects.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_contract: ^0.1.0
  adele_core_extensions: ^0.1.0

dev_dependencies:
  test: ^1.26.3
