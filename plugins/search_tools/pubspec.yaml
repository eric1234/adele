name: search_tools_plugin
description: Stock ADELE search tools for Session-authorized Environments.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.10.9 <4.0.0"

resolution: workspace

dependencies:
  adele_environment: ^0.1.0
  adele_model_tool: ^0.1.0
  adele_plugin_api: ^0.1.0

dev_dependencies:
  adele_product: ^0.1.0
  test: ^1.26.3
